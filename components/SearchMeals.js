import React, { useEffect, useState, useRef } from "react";
import {
  Animated,
  ActivityIndicator,
  Text,
  View,
  StyleSheet,
  Image,
  TouchableOpacity,
  Dimensions,
} from "react-native";

const SearchMeals = () => {
  return (
    <View>
      <Text>Meals</Text>
    </View>
  );
};

export default SearchMeals;
